query GetUserFollowing($address: String!, $limit: Int) {
  getUserFollowing(address: $address, limit: $limit) {
    id
    address
    displayName
    verified
    profile {
      username
      bio
      picture
      cover
    }
    socials {
      platform
      url
    }
  }
}
